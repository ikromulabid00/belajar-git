class Animal {
    constructor(name, sound){
        this.name = name;
        this.sound = sound;
    }

    action(){
        console.log(`says` + this.sound);
    }
}

// git init -> menginisialisasi prject git baru

// git add -> untuk menambahkan file kedalam staging area

// git commit -m (commit message) -> menambahkan file kedalam repository

// git status -> untuk melihat status perubahan

// git remote add oriign -> untuk menambahkan link remote repository

// git remote -v -> melihat remote yang ada

// git push -> untuk upload dari local repository ke remote repository 

// git pull -> download dari remote repository ke local repository